//cuda

nvcc -v

//anaconda

conda create -n 'name' python='version'
conda activate 
conda deactivate
conda info -e

//linux
ls
cd
touch 'name'
rm -r 'name'
rm -f 'name'
echo
expert

//ssh

ssh user.name@address
ssh kumakura@150.59.20.112

//sftp
sftp user.name@address
sftp kumakura@150.59.20.112

! (command) ローカル操作
get "name"
-r ディレクトリ











